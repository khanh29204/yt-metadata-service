[project]
name = "yt-metadata-service"
version = "0.1.0"
description = "Backend lấy metadata YouTube Music (yt-dlp + FastAPI)"
requires-python = ">=3.12"
dependencies = [
    "fastapi==0.141.1",
    "uvicorn[standard]==0.53.0",
    "yt-dlp",
]
